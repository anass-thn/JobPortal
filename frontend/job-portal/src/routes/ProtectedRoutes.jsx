import React from "react";
import { Navigate, Outlet, useLocation } from "react-router-dom";

const ProtectedRoutes = ({ requiredRole }) => {
    // will implemant later
    return <Outlet />;
};

export default ProtectedRoutes;